# Supabase (already configured — do not change)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Email notifications (optional but recommended)
# Sign up at https://resend.com and get an API key.
# Without these, inquiries are still saved to the database but no email is sent.
RESEND_API_KEY=
EMAIL_FROM=MK Water Industries <onboarding@resend.dev>
ADMIN_EMAIL=mkindustries0013@gmail.com
